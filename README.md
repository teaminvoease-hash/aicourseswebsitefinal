# System Status Checker

A very small website to quickly check whether the system is working or not.

## Run locally

```bash
python3 -m http.server 8000
```

Then open <http://localhost:8000> in your browser.
