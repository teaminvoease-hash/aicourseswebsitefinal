<?php $metaTitle='Reset Password'; include __DIR__.'/includes/header.php'; $token=$_GET['token']??'';
if($_SERVER['REQUEST_METHOD']==='POST'){if(!verify_csrf($_POST['csrf_token']??'')) die('Invalid CSRF'); $token=$_POST['token']; $r=$db->prepare('SELECT * FROM password_resets WHERE token=? AND used_at IS NULL AND expires_at > NOW()'); $r->execute([$token]); $row=$r->fetch(); if($row){$db->prepare('UPDATE students SET password_hash=? WHERE email=?')->execute([password_hash($_POST['password'],PASSWORD_DEFAULT),$row['email']]); $db->prepare('UPDATE password_resets SET used_at=NOW() WHERE id=?')->execute([$row['id']]); flash('success','Password updated. Please login.'); redirect('/login.php');} flash('error','Invalid or expired token');}
?>
<section class="container py-5" style="max-width:520px"><h1>Reset Password</h1><?php if($m=flash('error')):?><div class="alert alert-danger"><?=e($m)?></div><?php endif;?><form method="post" class="card card-body"><input type="hidden" name="csrf_token" value="<?=csrf_token()?>"><input type="hidden" name="token" value="<?=e($token)?>"><input type="password" required name="password" class="form-control mb-3" placeholder="New Password"><button class="btn btn-primary">Reset Password</button></form></section>
<?php include __DIR__.'/includes/footer.php'; ?>
